package org.yusufteker.routealarm.core.presentation

import androidx.compose.ui.graphics.Color


val WelcomeBackgroundColor = Color(0xFF0D1E2A)//0xFF16354b

object AppColors {
    // Background Rengi (Welcome Screen)
    val background = Color(0xFF16354b) // Koyu mavi tonunda bir arka plan rengi.

    // Button Background (Kahverengi)
    val buttonBackground = Color(0xFF7A4B3A) // Kahverengi tonunda bir buton arka planı.

    // Text Rengi (Beyaz / Açık)
    val textPrimary = Color(0xFFFFFFFF) // Başlıklar ve ana metinler için beyaz.

    // Text Rengi (Koyu)
    val textSecondary = Color(0xFFB0B0B0) // Metinler için daha soluk gri tonlar.

    // Card Arka Planı (Koyu tonlar)
    val cardBackground = Color(0xFF204865) // Kartlar için koyu mavi-yeşil tonları.

    // Border Rengi
    val border = Color(0xFF37474F) // Daha koyu gri tonlarında bir border.

    // Button Text Color
    val buttonText = Color(0xFFFFFFFF) // Buton metin rengi beyaz.

    val activeAlarmBarBackground = Color(0xFF122736)

    val textAlarmCard = Color(0xFFB0BEC5) // Açık mavi-gri ton

    val green = Color.Green

    val iconTint = Color.White


}

